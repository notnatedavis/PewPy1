# To Do :

- update Hue Detection of colors + avoid colors functionality
- openCV HSV (0-360,0-255,0-255) to regular (0-360, 0-100%, 0-100%) is causing me more issues than you would think , i wonder if I should simplify this into utils/ and have a dedicated function to reference (what would be more optimal efficient and modular?)
- modularize general_tab into debug_window w/ (notes)